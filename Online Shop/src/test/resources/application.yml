spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:OnlineShop
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode:
        always
      data-locations: data.sql
server:
  servlet:
    encoding:
      force: true